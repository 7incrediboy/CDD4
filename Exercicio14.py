#for = quando / while = if que repete / range = tamanho da string / len = quantidade do range no valor da string
nome = input("Digite seu nome:")
for i in range(0,len(nome),1):
    print(nome[i])
